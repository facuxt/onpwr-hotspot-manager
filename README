-Instalar dependencias con npm install.
-Configurar config.js con parametros del router, y usuario de hotspot que puede administrar el sistema.
-instalar nodemon npm install -g nodemon
-iniciar con nodemon server.js